module test {

main :: () -> I32 {
    // full loop
    acc := 0;
    for i := 0; i < 10; i += 1 {
        acc += i;
    }

    // missing initializer
    i := 0;
    for ; i < 10; i += 1 {
        acc += 1;
    }

    // missing incrementer
    for j := 0; j < 10; {
        j += 1;
    }

    // missing initializer and incrementer (while loop)
    k := 0;
    for ; k < 10; {
        k += 1;
    }

    // infinite loop
    for ;; {
        break;
    }

    return acc;
}

}
