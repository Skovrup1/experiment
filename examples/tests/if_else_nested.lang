main :: () -> S32 {
    x := 10;
    y := x;

    // bug with comments (look at scanner) :D
    // bug if ident literal is used in right operand in the binary expr
    // look at the expr statement code
    if x < y {
        if x < y {
            return 0;
        }
    }

    return 0;
}
