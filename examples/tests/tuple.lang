make_pair :: (a: S32, b: S32) -> (S32, S32) {
    return (a, b);
}

main :: () -> S32 {
    a, b := make_pair(1, 2);

    c, d :: (1, 2);

    a, b = (3, 4);

    e, f: (S32, S32) : (5, 6);

    e, f: (S32, S32) = (5, 6);

    ab := (1, 2);
    a = ab.0;

    return a;
}
